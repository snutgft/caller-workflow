name: caller-workflow
run-name: Run caller-workflow by ${{ github.actor }}
on:
  workflow_dispatch:
jobs:
  call-workflow:
    permissions:
      contents: write
      id-token: write
    uses: letsesign/called-workflow/.github/workflows/main.yml@c181ad4edf780f2f30066a5d2ceb1cb9fa8d3957
