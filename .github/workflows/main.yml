name: caller-workflow
run-name: Run caller-workflow by ${{ github.actor }}
on:
  workflow_dispatch:
jobs:
  call-workflow:
    permissions:
      contents: write
      id-token: write
    uses: letsesign/called-workflow/.github/workflows/main.yml@df9fae5c8ea34481b3f75097f252a36f8a2a1da8
