name: caller-workflow
run-name: Run caller-workflow by ${{ github.actor }}
on:
  workflow_dispatch:
jobs:
  call-workflow:
    permissions:
      contents: write
      id-token: write
    uses: letsesign/called-workflow/.github/workflows/main.yml@0ed1fb7e19306946783af6143784b7de650a868a
